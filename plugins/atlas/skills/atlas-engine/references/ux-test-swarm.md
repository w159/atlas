# UX Test Swarm

> **Moved.** The UX test swarm is owned solely by the `atlas-expedition` skill. Its
> full runbook, phase contract, agent roster, and gate definitions now live there.

Canonical home: `plugins/atlas/skills/atlas-expedition/` (invoke with `/atlas-expedition`,
or dispatch `atlas-expedition`). Use it for every UX/UI test-swarm run and for re-tests
after fixes. There is no separate UX-swarm agent roster in `atlas-engine` anymore; the
dedicated `ux-*` subagents were removed and their responsibilities fold into
atlas-expedition.

This file is retained only so older citations resolve. Do not treat it as an entry point.
