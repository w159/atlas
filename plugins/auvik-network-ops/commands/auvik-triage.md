---
description: Run the Auvik network triage workflow now and produce a ranked incident list.
---

Invoke the `network-triage` skill from the `auvik-network-ops` plugin. If the user provided a tenant name, scope to that tenant; otherwise run across all tenants. Produce the ranked Tier 1/2/Noise output in under 60 seconds by parallelizing alert enrichment.
