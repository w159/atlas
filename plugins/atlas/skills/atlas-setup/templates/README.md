# <project name>

<!-- Filled in by atlas-setup / atlas:docs-curator after stack detection. -->

One paragraph: what this project is and who it is for.

## Setup

```
<clone + install commands>
```

## Run / test / build

- Run:   `<command>`
- Test:  `<command>`
- Build: `<command>`

## Architecture overview

See `AGENTS.md` for agent-facing conventions and `docs/` for the full
project wiki (architecture, decisions, plans, specs).
