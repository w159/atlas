# AGENTS.md (docs/)

Deep orienting guidance that has outgrown the root `AGENTS.md`. Optional:
the root `AGENTS.md` is the minimum an agent needs. This file exists once
that minimum stops being enough.

## Full architecture

<!-- Module map, data flows, service boundaries. Filled in by
     atlas:docs-curator / atlas-audit as the architecture is mapped. -->

## Module map

<!-- One entry per top-level module: what it owns, what it depends on. -->

## Data flows

<!-- Request/response or event flows that cross module boundaries. -->

See `docs/architecture/` for the per-decision detail and ADRs behind
this map.
