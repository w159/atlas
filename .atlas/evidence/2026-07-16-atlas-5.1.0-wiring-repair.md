# Evidence: atlas 5.1.0 wiring-repair run (2026-07-16)

Session goal: "fix the atlas plugin so that it works better". Fixes applied to
plugins/atlas; every claim below carries the command actually run and its output.

## 1. Test suite green after all edits

```
$ cd plugins/atlas && python3 -m pytest hooks scripts -q
960 passed, 8 subtests passed in 6.95s
```

Baseline before edits was identical (960 passed, 7.56s), so no regression.

## 2. mcpServers pointer now resolves (was dangling)

Before: `.mcp.json` lived at `plugins/atlas/.claude-plugin/.mcp.json` while
`plugin.json` declared `"mcpServers": "./.mcp.json"`, which the Claude Code
plugin spec resolves relative to the plugin root
(https://code.claude.com/docs/en/plugins-reference#mcp-servers). Runtime
symptom: zero atlas connector servers registered in a live session (the only
plugin MCP servers present were context7, context-mode, ecc, claude-mem,
vercel).

After `git mv .claude-plugin/.mcp.json .mcp.json`:

```
$ python3 - <<'EOF'
import json, os
d = json.load(open('.claude-plugin/plugin.json'))
target = d['mcpServers']
print('mcpServers value:', target)
print('resolves from plugin root:', os.path.exists(target))
m = json.load(open(target))
print('servers:', sorted(m['mcpServers'].keys()))
EOF
mcpServers value: ./.mcp.json
resolves from plugin root: True
servers: ['auvik', 'blumira', 'cipp', 'connectwise', 'knowbe4', 'ninjaone',
          'paylocity', 'spanning', 'threatlocker', 'vanta']
all CLAUDE_PLUGIN_ROOT paths in .mcp.json resolve: OK
```

`scripts/test_connectors_wiring.py:16` updated to
`MCP_JSON = PLUGINS_ATLAS / ".mcp.json"`; suite still green (item 1).

## 3. Evidence-path contradiction removed

`hooks/completion_gate.py:73` reads evidence only from `.atlas/evidence/`, but
two agents directed writes to `docs/evidence/`:

- agents/ui-runtime-tester.md:22 -> now `.atlas/evidence/`
- agents/db-prober.md:24 -> now `.atlas/evidence/`

## 4. Operating-contract fallback anchored (14 skills)

```
$ grep -rln 'read `references/operating-contract.md`' skills | wc -l
0
$ grep -rl 'CLAUDE_PLUGIN_ROOT}/references/operating-contract.md' skills | wc -l
14
```

## 5. Banned Unicode sweep

```
$ python3 scan (U+2013/2014/2018/2019/201C/201D/2026 over *.md *.json *.sh)
banned-char lines remaining: 0
```

(18 lines across 5 files before the sweep.)

## 6. Legacy run-state markers removed

`plugins/atlas/docs/` contained only git-tracked `.run/atlas-metis.active` and
`.run/atlas-orchestrate.active` (pre-5.0 layout; 5.0.1 moved run state to
`.atlas/.run/`, ephemeral and gitignored). Tracked markers made the completion
gate treat plugins/atlas as a project root and made fresh clones look
permanently mid-orchestration.

```
$ git rm -r -q docs && git status --porcelain | grep 'atlas/docs'
D  plugins/atlas/docs/.run/atlas-metis.active
D  plugins/atlas/docs/.run/atlas-orchestrate.active
```

## 7. Smaller coherence fixes

- skills/atlas-launch/SKILL.md:3,36 - "atlas-audit or atlas-audit" rename
  residue removed.
- skills/atlas-audit/SKILL.md:49,72 - boundary text no longer defers to
  itself; names CODE vs ARCHITECTURE mode.
- skills/atlas-setup/SKILL.md:73 - scaffold tree moved audits/ under docs/,
  matching scaffold_docs.py:79 (WIKI_SUBDIRS_IN_ATLAS refuses .atlas/audits).
- skills/atlas-wiki/SKILL.md:31 - hardcoded /Users/... path made relative.
- skills/atlas-db-audit/SKILL.md:34,71 - readonly-SQL guard anchored at
  ${CLAUDE_PLUGIN_ROOT}/hooks/validate-readonly-query.sh.
- plugins/atlas/CHANGELOG.md - 5.1.0 entry added (manifest already said 5.1.0
  with no changelog entry).

## Refuted candidate findings (checked, not defects)

- "hooks scripts missing": all 10 hook scripts + scripts/atlas_doctor.py exist
  and are executable (ls of hooks/ and scripts/); earlier MISSING result was a
  shell-quoting artifact in the check itself.
- "atlas-loop/references/docs-ssot.md missing": file exists
  (ls skills/atlas-loop/references/ -> docs-ssot.md, loop-library.md).
- Root plugins/atlas/SKILL.md: valid per plugin spec (single-skill root
  placement is documented), heavily cited by the repo README; left in place.
